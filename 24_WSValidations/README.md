# WSValidations# WSValidations

The results generated by the validation of a network, for example from the `WSOpenNetwork.validate` method.

It is a collection of `WSValidation` objects, each of which represents a validation message that would appear in the user interface.

All methods in this class are read only.

## Availability

| Context | Available |
|---------|-----------|
| ![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) | Yes |
| ![UI](https://img.shields.io/badge/UI-green) | Yes |

---

## Methods Summary

| # | Method | Returns | Availability |
|---|--------|---------|--------------|
| 01 | [[] (Get Index)](#01-get-index) | `WSValidation?` | EXCHANGE, UI |
| 02 | [each](#02-each) | `WSValidation` | EXCHANGE, UI |
| 03 | [error_count](#03-error_count) | `Integer` | EXCHANGE, UI |
| 04 | [length](#04-length) | `Integer` | EXCHANGE, UI |
| 05 | [warning_count](#05-warning_count) | `Integer` | EXCHANGE, UI |

---

## Method Details

### 01. [] (Get Index)

```ruby
#[(index)] ⇒ WSValidation?
```

![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) ![UI](https://img.shields.io/badge/UI-green)

Returns the object from the collection at the specified index.

#### Parameters

| Name | Type | Description |
|------|------|-------------|
| `index` | `Integer` | The index requested (zero-based). |

#### Returns

`WSValidation?` — The object found, or `nil` if there is no object at this index.

---

### 02. each

```ruby
#each { |v| ... } ⇒ WSValidation
```

![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) ![UI](https://img.shields.io/badge/UI-green)

Iterates through the collection, yielding a `WSValidation` object.

#### Returns

`WSValidation` — Each validation message in the collection, yielded in turn.

#### Examples

```ruby
validations.each { |v| puts v.message }
```

```ruby
validations.each do |v|
  puts v.message
end
```

---

### 03. error_count

```ruby
#error_count ⇒ Integer
```

![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) ![UI](https://img.shields.io/badge/UI-green)

Returns the number of errors found during validation.

#### Returns

`Integer` — The count of validation messages with type `error`.

---

### 04. length

```ruby
#length ⇒ Integer
```

![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) ![UI](https://img.shields.io/badge/UI-green)

Returns the total number of validation messages.

#### Returns

`Integer` — The total count of validation messages in the collection.

---

### 05. warning_count

```ruby
#warning_count ⇒ Integer
```

![EXCHANGE](https://img.shields.io/badge/EXCHANGE-blue) ![UI](https://img.shields.io/badge/UI-green)

Returns the number of warnings found during validation.

#### Returns

`Integer` — The count of validation messages with type `warning`.

---

*Documentation generated from the official Autodesk InfoWorks ICM 2025 help pages.*
